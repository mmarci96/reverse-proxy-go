server:
  host: "0.0.0.0"
  listen_port: "8000"

static:
  dir: "./resources/static"

resources:
  - name: Radar proxy server
    endpoint: /radar/api/
    destination_url: "http://localhost:8080/api"
  - name: Tmdb proxy server
    endpoint: /tmdb/api/
    destination_url: "http://localhost:8090/api"
